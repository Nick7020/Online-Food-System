{
  "name": "food-express",
  "version": "1.0.0",
  "description": "Food delivery application",
  "scripts": {
    "start": "live-server --port=3000 --open=/login.html"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
